# Configuration file for IDProv provisioning protocol binding

# Listening address of the server when different from the hub mqtt server
# idpAddress: ""

# Listening port of the server. Default is 43776
idpPort: 9001

# Folder for certificate storage relative to home. Default is certstore
#idpCerts: "certstore"

# Instance ID of the client. Default is the plugin ID 'idprov-pb'
# clientID: "idprov-pb"

# Enable DNS-SD discovery publications. Default is enabled.
#enableDiscovery: true

# Certificate validity period in days. Default is 30
#validForDays: 30

# Services that IDProv supports
services:
  mqtt: mqtts://localhost:8883/ # must match hub.yaml
  mqttws: wss://localhost:8884/ # must match hub.yaml
  #wostdir: https://localhost:9002/   # must match wostdir.yaml
  #wostportal: https://localhost:993/  # must match wostportal.yaml
  #adminportal: https://localhost:994/ # must match adminportal.yaml
  #adminapi: https://localhost:995/  must match adminapi.yaml
