# Example hub configuration

# Message bus server
mqttAddress: "localhost" # LAN address to connect to the message bus
mqttCertPort: 33100 # certificate based auth over MQTT/TLS
mqttUnpwPortWS: 33101 # username password auth over WebSocket/TLS
mqttTimeout: 3

# Logging
logLevel: "info" # debug, info, warning, error. Default is warning

#configFolder: "./config" # plugin config, relative to the working directory
#certsFolder: "./certs" # set to activate TLS. Relative to app home folder

# Plugins to start
plugins: # list of plugins to start
  - /bin/ls # for testing
