# gateway.yaml WoST Gateway Configuration

# addres of the service bus
host: localhost:9678

# protocol, eg mqtt, nsq, ...  Default "" is the built-in websocket based service bus
protocol: ""

# list of plugins to launch
plugins: wost
  discovery
  directory
